export interface IImplement {
    id? : number,
    parent? : number,
    lid_id? : number,
    date? : string,
    master? : number,
    start_date? : string,
    end_date? : string,
    user? : number,
    note? : string,
    reason? : number,
    created_at? : string,
    updated_at? : string,
}
